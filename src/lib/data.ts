import type { Lead } from './types'

// This file is now deprecated in favor of fetching leads dynamically from NetSuite.
// It is kept for reference and potential fallback, but is no longer the primary data source.
export const LEADS: Lead[] = []
